export type Fund = {
  goal: number;
  funded: number;
  backers: number;
  bookmarked: boolean;
};

export const fundData: Fund = {
  goal: 100000,
  funded: 68093,
  backers: 5057,
  bookmarked: false,
};
