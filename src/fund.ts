export type Fund = {
  goal: number;
  funded: number;
  backers: number;
  bookmarked: boolean;
};

export const fundData: Fund = {
  goal: 100000,
  funded: 1223,
  backers: 5057,
  bookmarked: false,
};
